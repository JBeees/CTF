# TITLE : Taking LS
## Description
Just take the Ls. Check out this zip file and I be the flag will remain hidden.
## Solution
In this challenge we got a zip file, first i unzipped it. And there is a dir, a flag file and the password for the file. Make sure to use `ls -a` to list all file including hidden files. open the file using the password, will reveal the flag.
